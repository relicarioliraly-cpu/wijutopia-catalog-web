{
  "name": "wijutopia-catalog-web",
  "version": "1.0.0",
  "private": true,
  "description": "Catálogo Web Público de Wijutopia TCG - Frontend puro con Next.js + React 18",
  "scripts": {
    "install": "npm install",
    "dev": "next dev",
    "build": "NODE_OPTIONS='' next build",
    "start": "NODE_OPTIONS='' next start -p ${PORT:-3000}",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  }
}
