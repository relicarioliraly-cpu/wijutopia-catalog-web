{
  "name": "wijutopia-backend",
  "version": "1.0.0",
  "description": "API REST para la plataforma de comercio electrónico TCG de Wijutopia.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "lint": "node --check app.js && node --check config/db.js && node --check controllers/*.js && node --check middlewares/*.js && node --check routes/*.js && node --check services/*.js && node --check validators/*.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^2.4.3",
    "canvas": "^2.11.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^5.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
